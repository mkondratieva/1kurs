#include<stdio.h> 
#include<stdlib.h>
#include<time.h>
void pr(int *,size_t );  //печать массива
void init(int  **,size_t);  //заполнение массива случайными числами
int cmp(const void *, const void *); //сравнение элементов через указатели на них
